import { PageBody } from "~/shared";
import { UploadForm } from "./UploadForm";

export default function VideoUploadPage() {
  return (
    <PageBody className="justify-center">
      <UploadForm />
    </PageBody>
  );
}
